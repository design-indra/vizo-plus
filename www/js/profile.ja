function renderProfile(container) {
    document.getElementById('app-header').style.display = 'none';
    const user = auth.currentUser;
    container.innerHTML = `
        <div class="profile-section">
            <div class="avatar-big">${user.email[0].toUpperCase()}</div>
            <h2>${user.email}</h2>
            <div class="profile-card"><h4>🕒 Riwayat Tontonan</h4><p>Belum ada riwayat</p></div>
            <div class="profile-card"><h4>⭐ Favorit</h4><p>Belum ada favorit</p></div>
        </div>`;
}

function renderSidebar() {
    document.getElementById('sidebar').innerHTML = `
        <div class="sidebar-header"><h3>Pengaturan</h3><button onclick="toggleSidebar()">×</button></div>
        <ul class="sidebar-menu">
            <li>🌐 Bahasa</li>
            <li onclick="alert('Cache dibersihkan!')">🧹 Bersihkan Cache</li>
            <li>🛡️ Kebijakan Privasi</li>
            <li class="logout-item" onclick="auth.signOut()">🚪 Keluar</li>
        </ul>`;
}
